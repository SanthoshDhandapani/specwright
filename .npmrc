# Hoist Next.js + its plugin to the monorepo root node_modules as real
# packages (not pnpm symlinks). Without this, Turbopack rejects pnpm's
# symlinks pointing into ../../../node_modules/.pnpm/ as "outside the
# project directory" and refuses to compile.
public-hoist-pattern[]=next
public-hoist-pattern[]=@next/*
